{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}
<div class="accueil">
    <h1>Accueil</h1>
    <div class="descente-container">
        {% for i in 1..8 %}
            <div class="descente">
                <p>Descente {{ i }}</p>
                <p></p>
            </div>
        {% endfor %}
    </div>
</div>
{% endblock %}
